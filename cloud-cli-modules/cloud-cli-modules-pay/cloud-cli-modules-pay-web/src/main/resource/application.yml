eureka:
  client:
    service-url:
      defaultZone: http://localhost:1010/eureka/
spring:
  application:
    name: pay
server:
  port: 1070
feign:
   hystrix:
      enabled: true
hystrix:
   command:
      default:
         execution:
            timeout:
               enabled: true
